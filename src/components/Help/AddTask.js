import React from 'react';

function AddTask() {
  return (
    <div>
      <h3>Adding Tasks</h3>
      <p>The AddTask component provides users with a form to add new tasks to their todo list. It consists of input fields for entering task descriptions and selecting the task status. Users can input a brief description of the task they want to add and choose whether the task is completed or not. Upon submitting the form, the task is added to the list displayed on the main page. This component plays a crucial role in enhancing user productivity by allowing them to quickly add new tasks to their list.</p>
    </div>
  );
}

export default AddTask;
